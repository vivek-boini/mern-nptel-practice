# React Memoization Challenge

Challenge solution for "Memoization in React" case study. Implements TagList (React.memo + useMemo) and TagInput (useCallback) with performance optimization.

## Run

`npm install` then `npm run dev`

## Output

Open console. Click counter - no re-renders. Filter or add tags - selective re-renders only. Console logs verify memoization works.